#pragma once

#include <ntddk.h>

#define PRIORITY_BOOSTER_DEVICE 0x8000
#define PRIORITY_BOOSTER_DEVICE_TYPE PRIORITY_BOOSTER_DEVICE
#define IOCTL_PRIORITY_BOOSTER_SET_PRIORITY \
    CTL_CODE(PRIORITY_BOOSTER_DEVICE, 0x800, METHOD_NEITHER, FILE_ANY_ACCESS)
#define PRIORITY_BOOSTER_IOCTL_SET_PRIORITY IOCTL_PRIORITY_BOOSTER_SET_PRIORITY
#define PRIORITY_BOOSTER_IOCTL_QUERY_VERSION \
    CTL_CODE(PRIORITY_BOOSTER_DEVICE, 0x801, METHOD_BUFFERED, FILE_READ_DATA)

#define PRIORITY_BOOSTER_DEVICE_NAME L"\\Device\\PriorityBooster"
#define PRIORITY_BOOSTER_SYMBOLIC_LINK_NAME L"\\??\\PriorityBooster"

typedef struct _PRIORITY_BOOSTER_REQUEST {
    ULONG ProcessId;
    LONG Priority;
} PRIORITY_BOOSTER_REQUEST, *PPRIORITY_BOOSTER_REQUEST;

NTSTATUS IoCreateDevice(
    _In_ PDRIVER_OBJECT DriverObject,
    _In_ ULONG DeviceExtensionSize,
    _In_opt_ PUNICODE_STRING DeviceName,
    _In_ DEVICE_TYPE DeviceType,
    _In_ ULONG DeviceCharacteristics,
    _In_ BOOLEAN Exclusive,
    _Outptr_ PDEVICE_OBJECT *DeviceObject
);
